import React from 'react'
import ComponentY from './ComponentY'

const ComponentX = () => {
    return (
        <div>
            <ComponentY />
        </div>
    )
}

export default ComponentX























//React hooks provides a concept called Context

//React Context API allows you to easily access the data at different levels of the component tree, 
// without passing prop to every level.